'use client'
import { useEffect, useRef } from 'react'


export const ScrollTracker = () => {
  // Analytics tracking removed - will be handled by Netlify and n8n webhooks

  return null // This component doesn't render anything
}
